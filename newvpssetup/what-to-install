# this will eventually become a script

apt update
apt install tmux

wget https://golang.org/dl/go1.16.4.linux-amd64.tar.gz
rm -rf /usr/local/go && tar -C /usr/local -xzf go1.16.4.linux-amd64.tar.gz
echo "export PATH=$PATH:/usr/local/go/bin" | tee -a ~/.bashrc
source ~/.bashrc

echo "export PATH=$PATH:~/go/bin" | tee -a ~/.bashrc
go get -u github.com/tomnomnom/anew

go get -u github.com/tomnomnom/gf
#configure gf

go get -v github.com/projectdiscovery/httpx/cmd/httpx

curl https://raw.githubusercontent.com/felipecaon/403bypass/main/403bypass.sh > /usr/local/bin/403bypass
chmod +x /usr/local/bin/403bypass

go get -v github.com/projectdiscovery/nuclei/v2/cmd/nuclei
nuclei -update-templates

go get -u github.com/ffuf/ffuf
go get -u github.com/tomnomnom/fff
apt install nmap

apt install python3-pip -y
pip3 install s3scanner

wget https://github.com/blechschmidt/massdns/archive/refs/tags/1.0.0.tar.gz
# make this

go get -u github.com/tomnomnom/hacks/html-tool

go get -u github.com/tomnomnom/assetfinder
go get -v github.com/projectdiscovery/subfinder/v2/cmd/subfinder
#put apis tokens in subfinder
wget https://github.com/Findomain/Findomain/releases/download/4.3.0/findomain-linux
go get -u github.com/gwen001/github-subdomains

git clone https://github.com/felipecaon/BugBountyStuff

